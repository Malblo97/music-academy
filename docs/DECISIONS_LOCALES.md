# Décisions locales d'exécution (complète le registre §6 du Manuel)
| n° | Date | Décision | Pourquoi |
|---|---|---|---|
| 1 | 2026-07-XX | Mode: solo, 5 j/sem. Jalon « verrou n°2 vert M1+M2 » : 2026-09-XX | — |
| 2 | 2026-07-18 | `OUT = 00:01:36` (la vidéo dure 96 s de tournage, 90 s utiles + marge) manifest.json la remise | Recommendation IA |
| 5 | 2026-07-22 | Extraction des quiz : deux formats sources. (a) Format JSON complet (§29, quiz MVP historiques) : copie directe. (b) Format abrégé « Q / options (✓) / why » (§78–§85) : transcription structurée selon la grammaire Markdown → JSON documentée ci-dessous. Items `▶` sans notation textuelle dans la source : `play` laissé vide, item consigné dans `docs/qa/quiz-todo.md`. Champ `generator` (§29 quiz 3 et 7) copié tel quel, à valider en Phase 1. | Deux styles éditoriaux dans les sources sources. La grammaire du format abrégé est stable et déterministe — la transcription est mécanique, pas interprétative. |
| 6 | 2026-07-XX | Doublons éditoriaux des quiz M1 : le §29 (lot représentatif initial, ancienne numérotation) et le §84 (lot systématique M1 l09–l21, numérotation actuelle) produisent tous deux des quiz pour la leçon « tension/résolution » (§29-l14 = §84-l13) et la leçon « subV/emprunts » (§29-l20 = §84-l19). Ce sont deux écritures indépendantes, pas des révisions. Décision : le §84 gagne pour ces deux quiz (numérotation à jour, couverture systématique, intégratives recousues vers l01–l08). Les versions §29 sont archivées dans docs/production/archived-quizzes.md comme variantes alternatives. Les autres quiz §29 (m01-l01, m01-l05, m01-l25 sous réserve §85.1, m02-l02, m05-l08, m09-l04) sont extraits depuis §29 sans changement. | La CI Phase 1 exige un ID = un fichier. Deux versions du même quiz sous le même ID = collision. Le §84 est postérieur, systématique et cohérent avec la numérotation actuelle. |
| 8 | 2026-07-XX | Quiz M1 l01–l08 introuvables dans les sources §29–§86 (vérifié après lecture exhaustive). Contradiction avec Consolidation §1 (« 148 quiz ✅ »). Décision : consigner dans docs/qa/quiz-todo.md comme « à produire » ; ne PAS générer en Phase 0 ; production à planifier en Phase 1 selon la charte §4.3 du Manuel. | Règle d'or 3.0.4 : je copie, je ne produis pas. Si la source manque, la CI de Phase 1 signalera le manque via count.ts. |
| 11 | 2026-07-24 | Exercices à sous-parties (`submissionParts`) : la source fournit une solution de référence par sous-partie, avec un ID composite `mNN-sNN-<partId>` (exemples : m02-s30-elena, m02-s30-yours). Les sous-parties fermées ont une solution normative ; les sous-parties ouvertes (`userBrief: true`) ont une solution-gabarit (démonstrative, non standard de correction). Décision : un fichier solution par sous-partie, quel que soit son statut ; nom = ID composite exact ; champ `submissionPartId` ajouté dans le JSON pour routage ; `authorNotes` précise le statut (normatif ou gabarit) et pour les gabarits, documente le brief inventé et sa traduction en contraintes musicales. | La CI Phase 1 doit compiler chaque solution contre sa sous-partie. Les solutions-gabarit passent les contraintes lâches des sous-parties ouvertes. Le champ `authorNotes` porte la matière pédagogique différente selon le statut. |
| 12 | 2026-07-24 |	m02-e15 solution : retenir [F2+A3+A4], supprimer [F2+C4+A4] et le marqueur ??— non :	L'auteur marque explicitement la première version comme rejetée. La retenue omet la quinte, cohérente avec la facture à 3 voix du reste de la passe ; la rejetée était la triade complète — l'intention est donc bien la version marquée « non : », pas l'inverse
| 13 | 2026-07-24 |	m02-e15 solution : seconde harmonisation reconstituée (mes. 5–8), absente de s28	Une solution à 4 mesures viole lengthBars: [8,8] et harmonizationVariants: 2 de sa propre spec. Contenu non verbatim, marqué comme tel dans authorNotes. À re‑extraire si un s28 corrigé apparaît ; à re‑valider contre l'analyseur en Phase 1
| 14 | 2026-07-24 |	m02-e15 : authorNotes réécrites	La note d'origine décrivait des « triades complètes » et un Dm absents de la notation de s28. Elle documentait la solution complète, non l'extrait. Conservée comme source d'intention (le Dm), pas comme description
| 15 | 2026-07-31 | Champ `module` du frontmatter des fiches/leçons : la forme canonique est le nom du dossier réel sous `packages/content/modules/`, pas un nom éditorial de source. Les 11 fiches M5 déclaraient `module-05-orchestration` (dossier réel : `module-05-instrumentation`) et les 4 leçons M9 déclaraient `module-09-film-genres` (dossier réel : `module-09-genres`). Les 15 fichiers ont été corrigés pour déclarer `module-05-instrumentation` / `module-09-genres`. Les `id` (`m05-l0X-...`, `m09-l0X-...`) restent inchangés — seul le champ `module` était erroné. | §7.2 : les IDs sont immuables, mais rien n'impose que `module` copie un intitulé de source rédactionnelle ; la charte de content-sync (Phase 2) résout le module d'appartenance par nom de dossier, pas par ce champ déclaratif. La divergence aurait fait rejeter ou orpheliner ces 15 leçons au sync. |
| 16 | 2026-07-31 | `generator/recipes/` : les 8 recettes (`G-M48`, `G-05`, `G-07`, `G-08`, `G-W1..5`) annoncées par §77 n'existent NULLE PART sous forme de JSON littéral — ni dans s77, ni dans un autre document, ni dans un commit supprimé (vérifié par `git log --all`). §77.2 ne les décrit qu'en PROSE (durées, sections, comptes d'occurrences, traits de genre, mais pas de seed ni de cellule de motif notée). Décision, sur arbitrage explicite : au lieu de les consigner comme « à produire » (cf. décision n°8), les 8 fichiers ont été rédigés maintenant, au schéma `GeneratorRecipe` de §77.0, en **dérivant tout ce qui est déductible** des specs/solutions M11 croisées (`m11-e01/05/06/07/08`, `m01-e48`) et en **inventant explicitement le reste** (seed, tempo quand non donné, cellule de motif, positions de mesure fines, instrumentation, rolePlan). Chaque fichier porte `"status": "draft-placeholder"` et un bloc `_provenance` qui liste, champ par champ, ce qui est extrait (avec sa source) vs inventé. Point ouvert non résolu : `m01-e48-solo-analysis.json` décrit encore une pièce statique de 16 mesures (`assets/m01-e48-piece.json`, inexistant), texte antérieur à D-P15 et jamais mis à jour vers `G-M48` (24 mesures, mes. 19) — à trancher en Phase 1. | Le contenu doit être versionné dès Phase 0 (§4.3, tutoriel 3.5) et le verrou CI n°5 (auto-cohérence du générateur) a besoin d'un artefact à mordre en semaine 8. Règle d'or 3.0.4 (« je copie, je ne produis pas ») s'applique normalement au manque de source (cf. décision n°8) ; ici l'utilisateur a explicitement arbitré pour une rédaction dérivée-et-documentée plutôt qu'un blocage, à condition que rien d'inventé ne soit présenté comme extrait. Ces recettes sont des brouillons de travail, pas des données vérifiées par un moteur réel (le générateur n'est encore que spécifié, pas implémenté) — à revalider en Phase 1.5/6. |
| 17 | 2026-07-31 | Round-trip des `notation` de solutions (verrou n°3) : `packages/music-core/scripts/analyze.ts` (nouveau, périmètre : tsx, réutilise `parseNotation`/`printNotation` réels) scanne les 165 chaînes `notation` de tout `packages/content/solutions/`. Trouvé et corrigé **8 erreurs dures F-21** (liaison sans cible), pas 4 : `m03-e05` ×3 (le `~` était posé une mesure trop tard — accord tenu m6→m7 mal indexé m7→m8, chaque fois signalé dans les `authorNotes` elles-mêmes comme « tenu »), `m04-e05-fourth-species2` ×1 et `m04-e09-imitation-machine3` (voix V2) : une dissonance qui **résout** mélodiquement vers une autre hauteur portait par erreur un `~` (les `authorNotes` décrivent explicitement une résolution, pas une tenue), et `m07-e03-the-carpet` ×3 (2 rapportées immédiatement + 1 démasquée seulement après correction des 2 premières, car `parseNotation` s'arrête à la première liaison sans cible : `analyze.ts` a dû tourner en boucle fixer→rescanner). Dans `m07-e03`, 6 des 11 `~` déclarés n'avaient aucune cible harmonique commune (Fadd9→G7sus4 aux altos, Cmaj7→Fm aux 2èmes violons — vérifié accord par accord contre le champ `harmony` de la solution) : supprimés plutôt que déplacés, et les `authorNotes` (« onze fils tenus ») corrigées à 5, le compte réel. **Aucune « divergence d'impression »** (round-trip `parse→print→parse` changeant le contenu audible) n'a été trouvée ailleurs dans le corpus : hypothèse retenue pour le chiffre de 11 rapporté par l'utilisateur — il s'agit très probablement du compte erroné des `~` dans les `authorNotes` de `m07-e03` lui-même (11 liaisons déclarées, 6 invalides), pas de 11 fichiers distincts. `count.ts` et la suite `F-52` de `music-core` restent verts après coup. | Le verrou CI n°3 (round-trip notation) n'existait qu'en test unitaire sur des `Note[]` synthétiques (`test/notation.roundtrip.test.ts`) — rien ne validait encore le contenu réel copié en Phase 0. `analyze.ts` comble ce trou et reste dans le dépôt comme outil de triage réutilisable avant la semaine 5 (verrou n°2 sur M3). Le fait qu'un fix expose une erreur suivante (masquée par l'arrêt au premier throw) est resté sans script, d'où l'itération manuelle documentée ici plutôt qu'un chiffre de départ pris pour argent comptant. |
| 18 | 2026-07-31 | Renommage des 204 fichiers `solutions/**/*.json` de `mXX-eNN[-suffixe-numérique]-slug.json` vers `mXX-sNN-slug[-variantId\|partId].json` (`git mv`, contenu byte-identique, `exerciseId` interne inchangé). Convention retenue (validée par l'utilisateur, alignée sur `docs/production/s63/s64/s65/s89/s77-*.md` déjà en `mXX-sNN`) : un compteur global par module, en ordre `eNN` croissant ; chaque exercice réserve `max(1, nb_solutions)` créneaux consécutifs (donc un exercice à 0 solution, ex. `m03-e01` en QUIZ à réponses embarquées, réserve quand même 1 créneau silencieux — c'est pourquoi `m03-e02` devient `s02` et non `s01`) ; l'ordre des variantes dans un exercice multi-solutions suit `spec.variants[]`/`spec.parts[]`/`spec.submissionParts[]` de la fiche d'exercice (secours : ordre des suffixes de fichier existants, pour 6 exercices sans tableau de variantes déclaré — vérifié sans divergence). Script jetable (`rename_plan.js`/`gen_mv.js`, scratchpad, non versionnés) : 0 collision de nom cible, 204/204 = la cible `count.ts`, reproduit à l'identique le triplet `m03-s05/06/07` déjà renommé manuellement avant cette session. **Effet de bord découvert, sans rapport avec le renommage** : `pnpm --filter @ma/music-core test` fait apparaître 11 échecs dans `test/locks/roundtrip.test.ts` (verrou n°3, variante stricte : `printNotation(parseNotation(x))` doit égaler `normalize(x)`, pas seulement préserver le contenu audible) — `m03-s16`, `m03-s19`, `m03-s23`, `m04-s19`, `m04-s23`, `m05-s06`, `m08-s01`, `m08-s05`, `m08-s20`, `m09-s01`, `m10-s04`. Contenu vérifié byte-identique à `HEAD` (aucun de ces fichiers n'a été touché par les corrections F-21 de la décision n°17) : des tenues/silences écrits en jetons fragmentés (`F4:e~ F4:e`) au lieu du jeton canonique le plus long (`F4:q`) — un défaut de forme, pas de fond, distinct de F-21. Pré-existant, non corrigé ici (hors périmètre de la demande) — **à trier avant le verrou n°3 réel**, comme les 15 notations de la décision n°17. | Convention demandée explicitement par l'utilisateur après clarification (l'option « eNN→sNN littéral » aurait cassé sur tout exercice à variantes). `analyze.ts` (décision n°17) ne couvre que l'équivalence audible du round-trip ; `test/locks/roundtrip.test.ts`, découvert seulement en lançant la suite complète après le renommage, est plus strict et révèle une classe d'erreurs différente (regroupement de durées non canonique) que `analyze.ts` ne détecte pas — les deux verrous sont complémentaires, aucun ne remplace l'autre. |
| 19 | 2026-08-01 | M2 : compte définitif = **28 solutions**, pas 29. La Consolidation (F-9, §5) et le tutoriel Phase 0 (§3.2.a, §3.3.c) annonçaient 29 = 25×1 + e20×2 (variantes `retenue`/`lyrique`) + e30×2 (parts `elena`/`yours`). Seule la variante `lyrique` de `m02-e20-sad-two-ways` a été produite et copiée (`m02-s20-sad-two-ways.json`) ; il n'existe pas d'exercice/solution `retenue` distinct — le texte « s20-retenue » de `docs/production/s28-m02soluces.md` est une production non retenue, pas un fichier manquant à recopier (confirmé par l'utilisateur : tous les exercices de M2 sont dans le dossier `exercises` du module, leurs solutions dans `solutions/m02`, sans pièce manquante). `count.ts` (cible `module-02-melodie.solutions: 28`) reflète donc l'état réel et définitif. La Consolidation et le tutoriel Phase 0 gardent 29 dans leur texte (documents de production historiques, non corrigés rétroactivement) mais n'ont plus valeur de cible. | La cible de `count.ts` avait été abaissée de 29 à 28 sans entrée dans DECISIONS_LOCALES — contradiction avec la règle d'or (toute divergence avec les livrables annoncés doit être consignée, cf. décisions n°8 et 16). Cette entrée documente la divergence trouvée et sa résolution, pour qu'un futur audit ne rouvre pas la question. |
| 20 | 2026-08-01 | **Convention définitive de nommage des solutions, amende et remplace le compteur positionnel de la décision n°18** : `mNN-sXX-<slug-exercice>[-<variante>].json`, où `XX` = numéro de l'exercice (`eXX`), **immuable**, et non un compteur global séquentiel ; `<variante>` obligatoire dès qu'un exercice a plus d'une solution (identifiant issu de `spec.variants[].id` / `spec.parts[].id` / `spec.submissionParts[].partId`, ou à défaut du suffixe distinctif déjà présent dans le nom de fichier). Généralise la décision n°11 (`mNN-sNN-<partId>`, déjà écrite avec `sNN` = numéro d'exercice) à toutes les formes de multi-solutions (variantes, parts, cas ad hoc comme `m03-e05` à 3 solutions). Le compteur positionnel de la décision n°18 est abandonné : il rendait `sXX` dépendant de l'ordre et du nombre de solutions des exercices précédents, donc instable à toute insertion future (M6/M10/M9 génériques à venir) — exactement le risque que **F-30** (renvoi « mes. 1–8 de s30-elena ») et **F-37**/**D-P17** (`given` inter-modules pointant une solution de référence) rendent coûteux si un numéro venait à bouger. **Migration exécutée** : script d'inventaire jetable (scratchpad, non versionné) construit la cible pour les 204 solutions à partir du seul `exerciseId` interne (indépendant du nom de fichier) ; 0 collision, 0 problème de correspondance slug/variante. 102 fichiers déjà conformes (M1, M2, M9, M10, M11) ; 102 renommés (M3, M4, M5, M6, M7, M8) via `git mv` en deux passes (nom temporaire puis nom final, pour éviter toute collision de chemin intermédiaire pendant le renommage groupé). Cas particulier : `m02-s31-elena-and-you-yours.json` → `m02-s30-elena-and-you-yours.json` (l'exercice 30 a bien deux solutions `s30-*-elena`/`s30-*-yours`, comme l'exigeait déjà la décision n°11 — l'ancien nom la violait). Trois fichiers de `m03-e05` (`secret-passage-{ger6-v7,dim7,augmented}`) étaient encore non suivis par git (travail non commité d'une session antérieure) : renommés par `mv` simple, pas `git mv`, sans impact sur le contenu. `pnpm content:count` reste vert (204/204, mêmes cibles par module) après renommage — le script ne valide que le contenu des `exerciseId` internes, jamais le nom de fichier. | Décision utilisateur explicite du 2026-08-01 après audit du mélange des deux conventions dans le dépôt (options 1 et 2, 102 fichiers chacune) : l'argument décisif est que `sXX` = numéro d'exercice est stable par construction (l'unicité du nom vient déjà du slug + de la variante, pas du numéro), alors qu'un compteur positionnel casse tous les renvois existants dès qu'un exercice est inséré au milieu d'un module — un risque concret vu le volume de contenu encore à produire (genres, fiches M5, etc.). |
| 21 | 2026-08-01 | M10 : `kind: "DAW_MISSION"` manquait sur les **15/15** specs (`packages/content/modules/module-10-cubase/exercises/m10-e01..e15.json`) — vérifié : aucun autre module du dépôt n'a de fichier d'exercice sans `kind`. Cause : `docs/production/s23.3-m10exercices.md` §« Conventions du lot » déclare `kind: "DAW_MISSION"` comme un **champ par défaut** (« omis quand identiques » dans le document, au même titre que `skills`/`rubric`/`isGenerated`) — l'extraction Phase 0 a lu « omis » comme « à ne pas copier » au lieu de « à ajouter tel quel, non répété dans chaque bloc JSON de la source ». Or `kind` est le discriminant polymorphe du verrou n°2 (F-48 : score / preuve / checklist / concordance) et du futur runner — sans lui, aucun routage n'est possible, contrairement à `skills`/`rubric`/`isGenerated` qui ont des défauts applicables côté schéma. Correctif : `"kind": "DAW_MISSION"` ajouté après `title` dans les 15 fichiers (valeur explicitement donnée par la source, verbatim — aucune invention). `skills`/`rubric` restent omis là où la source les omet (e01, e06, e09, e10, e13, e14 : valeurs par défaut du lot, pas encore de schéma Zod pour les appliquer, hors périmètre de cette correction). `pnpm content:count` reste vert (15/15/15/15) après coup — le script ne valide pas encore la présence de `kind` (aucun verrou dédié pour l'instant, à ajouter en Phase 1 avec le schéma `DAW_MISSION`). | Signalé par l'utilisateur : verrou n°2 polymorphe par `kind`, routage impossible sans lui. Cette entrée documente pourquoi le champ manquait (mélecture d'une convention de rédaction, pas un oubli de contenu) et pourquoi seul `kind` a été restauré ici, pas les autres défauts du lot (`skills`/`rubric`/`isGenerated` ont un filet de sécurité schéma à venir ; `kind` n'en a aucun, c'est un discriminant). |
| 22 | 2026-08-01 | `packages/music-core/scripts/analyze.ts` complété avec le mode fichier unique exigé par le tutoriel Phase 1 (**S1.J5**, v0) : `pnpm analyze <chemin-solution.json>` parse le(s) champ(s) `notation` du fichier et imprime nb de notes, ambitus (noms + demi-tons), durée en mesures, et la notation régénérée. Le fichier ne contenait jusqu'ici que l'outil de la décision n°17 (`scanCorpus`, un balayage batch de tout le corpus pour détecter erreurs dures et divergences de round-trip) — utile mais différent du « stéthoscope à un fichier » demandé par S1.J5 et jamais écrit. Les deux cohabitent maintenant dans le même fichier : `pnpm analyze` sans argument garde le comportement batch existant (165 notations, 0 erreur, inchangé) ; `pnpm analyze <path>` bascule sur le mode v0. Réutilise `meterById`/`collectNotations`/`parseMeter` déjà écrits pour le mode batch (une seule indexation des mètres d'exercices, pas de duplication). Vérifié : `tsc --noEmit` et `eslint` verts sur le fichier (non gaté en CI — `"lint": "eslint src test"` ne couvre pas `scripts/`), testé sur un cas simple (`m02-s02`, notation top-level) et un cas sans notation (`m10-s01`, mission déclarative) sans erreur. | Signalé par l'utilisateur : le tutoriel Phase 1 marque S1.J5 comme le jalon de fin de semaine 1 (« analyze répond ») et le décrit comme l'outil de diagnostic de toute la suite (il s'enrichira jusqu'à S8 avec `--spec`/`--live`/`--midi`/`--recipe`) — cette version couvre uniquement la notation, seule brique du moteur qui existe à ce stade. |
| 23 | 2026-08-01 | `module.json` créés pour **M3, M4, M6, M7, M8, M10, M11** (manquaient — seuls M1/M2/M5/M9 en avaient). `id`/`order` = nom de dossier / numéro de module (sans ambiguïté). `title` repris des en-têtes `## 5.N` du Manuel (plus précis que le tableau §5.0 : ex. M10 = « Cubase Pro 13 / Real-World Workflow », pas le résumé « Cubase / workflow réel »). `tier: "V1"` pour les 7 (le tableau §5.0 les classe tous V1, vs `"MVP"` pour M1/M2/M5/M9). `minLevel` : **M3, M4 → 0** — `docs/production/s04-core.md` §Gating : « les modules 1→4 sont séquentiels » (même mécanisme que M1/M2, gating par leçon précédente complétée, pas par XP — cohérent avec leur `minLevel: 0` déjà en place). **M6, M7, M8, M10 → 9** — même source : « 5→10 s'ouvrent en parallèle dès le niveau requis », le même palier que M5/M9 (déjà `minLevel: 9`, ancré Manuel §1.5 : « ≈ niveau 9–10 pour ~6 600 XP cumulés »). **M11 → 18**, seule valeur non documentée littéralement (le Manuel dit juste « exige le maximum d'acquis », dépendance « tout » au tableau §5.0, mais aucun chiffre — M11 est explicitement hors du groupe « 5→10 »). Dérivée avec l'utilisateur à partir des données réelles du dépôt (pas inventée) : somme des `xpReward` de tous les exercices de chaque module — M1+M2 = 6 730 XP (≈ l'ancre « ~6 600 » du Manuel, confirmant la méthode), M1→M10 = 18 740 XP soit ×2,8 ; en appliquant la même courbe que l'ancre (`seuil(n) = 100×n^1.6`, s02-backend.md — niveau ∝ XP^(1/1.6)) au ratio, niveau ≈ 9,5 × 2,8^(1/1,6) ≈ 18. Reste dans la fourchette `level 1..30` du schéma Prisma (s02-backend.md). **Incohérence relevée en passant, non corrigée ici** : la formule `seuil(n) = 100×n^1.6` appliquée littéralement à 6 730 XP cumulés donne niveau ≈ 13,7, pas 9–10 comme l'affirme le Manuel §1.5 — les deux documents de conception (formule vs ancre narrative) ne se réconcilient pas exactement ; on a raisonné par ratio sur l'ancre plutôt que sur la formule brute, pour rester cohérent avec le seul chiffre que le Manuel affirme explicitement. À trancher en Phase 1 quand `xp.rules.ts` sera écrit. | Signalé par l'utilisateur : le gating §1.5 dépend de `order`/`tier`/`minLevel`, absents pour 7 modules sur 12. M3/M4/M6/M7/M8/M10 sont directement lisibles depuis des affirmations explicites du Manuel et de s04-core.md — pas une invention. M11 n'avait aucune source ; question posée explicitement à l'utilisateur (« à chiffrer ensemble ») plutôt que d'inventer un chiffre, conformément à la règle d'or — la valeur retenue est calculée à partir de données réelles du dépôt et de la seule ancre chiffrée existante, pas choisie arbitrairement. |
| 24 | 2026-08-01 | `docs/qa/` était vide : `quiz-todo.md` (décisions n°5 et 8), `docs/production/archived-quizzes.md` (décision n°6) et les tables de suivi d'extraction (`docs/qa/extraction-mNN.md`, promises par le tutoriel Phase 0 §2.1/§3.x) n'existaient pas alors que trois décisions déjà consignées y font référence comme à des fichiers réels. Les trois ont été reconstruits en scannant l'état réel du dépôt (aucune invention) : **(a)** `docs/qa/extraction-m01.md` → `extraction-m11.md`, une table leçon/specs/solutions/quiz par module, générée depuis les fichiers réels (recoupée avec les cibles `count.ts` — identique partout) ; **(b)** `docs/qa/quiz-todo.md` : 41 items `▶` sans `play` trouvés par balayage de tous les `*-quiz.json` (M6 15 · M7 10 · M8 2 · M10 9 · M11 5 — zéro en M1–M5/M9), tous du même profil non anticipé par la décision n°5 : des comparaisons de rendu sonore/mix, pas des notations manquantes — non tranché, à statuer en Phase 1/2 ; **(c)** `docs/production/archived-quizzes.md` : les deux quiz §29 (m01-l14 tension/résolution, m01-l20 substitutions) que la décision n°6 dit avoir archivés. **Deux écarts découverts en reconstruisant, tous deux non corrigés ici (hors périmètre de la demande, qui portait sur la scaffolding manquante, pas sur le contenu) :** (i) la décision n°8 (« quiz M1 l01–l08 introuvables ») est **stale** — `docs/production/s29-m05l01LotQuiz.md` (§29.2) et `s29.3-m01Quizzes.md` couvrent les 8 depuis le début, et les 25/25 quiz M1 sont bien commis avec du contenu réel (vérifié par item count contre les en-têtes de `s29.3`) ; la décision n°8 a dû être écrite avant la découverte de `s29.3`, ou sans la croiser. **Ce point est clos par la présente entrée** — `quiz-todo.md` §2 le documente, aucune production n'est due. (ii) La décision n°6 (« §84 gagne ») n'a été exécutée qu'à moitié : les fichiers commis `m01-l14-quiz.json`/`m01-l20-quiz.json` contiennent encore le texte **§29** (vérifié octet pour octet), pas la version §84 (`s84-lotQuiz7.md`, format abrégé, lignes 35–41 et 78–83) que la décision déclarait gagnante. **Ce point reste ouvert** — décision à reprendre : soit transcrire §84 en JSON complet et remplacer les deux fichiers (le §29 archivé restant alors correct tel quel), soit ratifier §29 comme version définitive et corriger le texte de la décision n°6 en conséquence. | Signalé par l'utilisateur : « la traçabilité promise (chaque manque consigné) est rompue » — trois décisions pointaient vers des fichiers qui n'ont jamais été créés. En reconstruisant fidèlement le contenu que ces décisions annonçaient, deux divergences supplémentaires entre le texte des décisions et l'état réel du dépôt sont apparues (même famille de bug que la décision n°19 sur M2 et la n°21 sur `kind: DAW_MISSION`) — consignées plutôt que corrigées silencieusement, pour ne pas modifier du contenu live (quiz m01-l14/l20) sans confirmation explicite. |
| 25 | 2026-08-01 | Verrou n°3 (S1.J4) : 17 fixtures actives sur 18 prescrites par le tutoriel Phase 1 — écart désormais résolu. `test/fixtures/notation/solutions.test.ts` avait la 18e (`solution-m03-s02`) en `it.skip`, avec un commentaire explicite : « BLOQUÉ. `packages/content/modules/module-03-*` et `packages/content/solutions/m03` n'existent pas dans le monorepo à ce jour ». Ce n'est plus vrai — M3 a depuis été extrait en Phase 0 (31 solutions, cf. décisions n°18/20). Correctif : dé-skippé et implémenté contre `packages/content/solutions/m03/m03-s02-solemn-shadow.json` (`exerciseId: m03-e02-solemn-shadow`, 8 accords de 4 notes = 32 notes, conforme à la fixture attendue) ; nom de fichier réel `m03-s02-*` et non `m03-e02*` comme l'écrivait le tutoriel — la convention de nommage a changé entre-temps (décision n°20), ce n'est pas le même exercice sous un autre nom, juste le même exercice sous son nom post-renommage. `pnpm --filter @ma/music-core test` : 18/18 fixtures notation vertes (150/161 tests globaux, +1 par rapport à avant ce correctif) ; les 11 échecs restants sont ceux, déjà connus et sans rapport, de la décision n°18 (jetons de durée fragmentés, verrou n°3 strict). | Signalé par l'utilisateur (S1.J4). Le blocage documenté dans le code était honnête et correct au moment où il a été écrit (M3 n'existait pas encore) — la règle d'or interdisait d'inventer une notation M3 à l'époque. Une fois la source réelle disponible (Phase 0 complétée pour M3), le blocage devient un TODO obsolète à lever, pas une décision de conception à reconsidérer. |
| 26 | 2026-08-01 | S2.J1-J3 implémentés : `analyzers/key.ts` (+ `data/krumhansl.ts`), `data/chordForms.ts` + `analyzers/chord.ts`, `analyzers/cadence.ts` (+ `functionOf`) — les trois fichiers étaient vides (`key.ts` ne contenait qu'un copier-coller cassé du tutoriel). 52 fixtures vertes (8 clé, 35 accord, 9 cadence). Décisions de spec introduites, non explicitées par le tutoriel : **(1)** `Mode` (`types.ts`) = 7 valeurs (`major/minor` + les 5 modes exotiques) — `estimateKey` ne produit jamais `ionian`/`aeolian`, ce sont `major`/`minor`. **(2)** `PedalPlan.ranges` en TICKS (mêmes unités que `Note.start`), pas en numéros de mesure — la conversion mesure→ticks reste à la charge de l'appelant (qui connaît le mètre) ; évite d'imposer un mètre à `detectChord`. **(3)** `TimedChord` porte `notes` (les notes sonnant sur la fenêtre, pour la voix de soprano — seule condition des 4 de la cadence parfaite, l16 M1, que `ChordResult` seul ne porte pas) et `idiomTags?` (F-16 : l'idiom-tagger de S2.J4, pas encore écrit, y postera `'aug6'` — en attendant, testé en fournissant le tag directement dans la fixture). **(4)** `classifyNonChordTone` implémente la table des six de m02-l08 §3 (« la table générale... que `detectChord` utilise pour qualifier ») : Retard/Appoggiature se distinguent uniquement par l'arrivée (tenue-préparée vs saut/directe), les 4 autres par position + direction. **(5)** `functionOf` classe par DEGRÉ du root (jamais par forme/qualité de l'accord) — c'est ce qui empêche par construction le piège F-16 (un accord "de forme dominante" hors degré V n'est jamais confondu avec V, cf. `ger6-half-not-subv-f16` où Eb7 en sol mineur reste `functionOf='T'` par son root ♭6̂, pas D) ; ♭VII est classé D (dominante de rechange, « back-door », cohérent avec l'idiome `back-door` prévu en S2.J4). **(6)** Faute de paramètre `meter` dans les signatures du tutoriel (`estimateKey`, `detectCadences`), F-5/F-19.b/F-2 utilisent un mètre par défaut 4/4 (1920 ticks/mesure) — même convention que `solutions.test.ts` (aucune solution M1-M3 ne déclare de mètre). **(7)** F-19 corrigée en cours de test : la relecture initiale ne rebasculait que si l'ancrage DIFFÉRAIT de la tonique Krumhansl brute — `d-dorian-bourdon` et `phrygian-anchor` échouaient (Krumhansl trouvait déjà la bonne tonique mais le mauvais MODE, faute de sensible) ; corrigé pour toujours re-vérifier la collection diatonique autour de l'ancrage, tonique Krumhansl fausse ou pas. **(8)** La fixture `window` (m03-s04-four-faces) : le tutoriel cite « mes. 9-14 », mais la pièce réelle ne fait que 12 mesures — fenêtre alignée sur mes. 8-12 (le texte des `authorNotes`, « Confirmation m9–12 » + la sortie du pivot m8) plutôt que sur la référence obsolète. Suite naturelle (hors périmètre demandé) : S2.J4 `collection.ts`/`idioms.ts` — le tag `aug6` que `cadence.ts` sait déjà lire n'est encore posé par rien. | Demande explicite de l'utilisateur (« à l'aide du tutoriel phase 1... fais-moi S2.J1, S2.J2, S2.J3 »). Chaque choix ci-dessus comble un blanc du tutoriel (signatures exactes données, mécanique interne au programmeur) — consignés pour que S2.J4+ les réutilise sans redécouvrir les mêmes arbitrages, et pour que `@ma/shared` (si le schéma Zod y reprend `PedalPlan`/`TimedChord`) parte des mêmes unités. |
| 27 | 2026-08-02 | **M5 rangé par famille et porté à 26 fiches.** (a) *Renumérotation* (2026-08-01) : les 11 fiches MVP sont réordonnées par famille (cordes → bois → cuivres → claviers → percussions) ; 6 changent de numéro (`git mv`, contenu inchangé hors `id`/`QuizBlock`) — flûte l06→l07, hautbois l07→l10, clarinette l08→l12, trompette l09→l16, cor l12→l17, piano l10→l21. Les `id` de leçon ne sont donc PAS immuables ici, contrairement aux `id` d'exercice/solution (décision n°20) : rien dans §7.2 ne verrouille l'ordre d'un module encore en production, et le coût du changement est borné car chaque renvoi est textuel. Les 24 renvois croisés ont été repris (intra-M5 + M4 `l11`, M6 `l14-quiz`, M7 `l02/l03/l04/l05/l06/l08/l09` et leurs quiz, M8 `l12`, M10 `l04` et son quiz) ; `m05-e08-horn-roles.json` repointe `lessonId: m05-l17-french-horn`. (b) *Extraction du lot V1* (2026-08-02) : les **15 fiches** de `docs/production/s96-m05Fiches.md` sont copiées par script de découpage jetable (scratchpad, non versionné) avec vérification de round-trip — la concaténation des 15 blocs rend la source à l'identique, aucune re-saisie manuelle. (c) **Écart de périmètre assumé** : la liste d'instruments initiale laissait entendre 26 fiches nouvelles (une par percussion), et 26 scaffolds vides avaient été ouverts en ce sens. La source les **regroupe en deux fiches** — `l25-claviers-de-percussion` (glockenspiel, xylophone, vibraphone, marimba) et `l26-percussions-orchestrales` (caisse claire, grosse caisse, cymbales, triangle, tam-tam, tambourin, woodblocks, castagnettes, claves) — ce qui correspond à la structure imbriquée de la demande d'origine. Les 13 scaffolds devenus sans objet (`l25-glockenspiel`…`l37-claves`) ont été supprimés ; le module tient en **26 leçons** (`l01`–`l26`, séquence continue, vérifiée sans trou) et non 37. `count.ts` : cible `lessons` 11 → 37 → **26**. (d) **Deux manques laissés ouverts, non corrigés ici.** Les 15 fiches déclarent chacune un `QuizBlock` mais `s96` **ne contient aucun quiz** : 15 renvois sans fichier, 75 items à produire — silencieux en CI, car `count.ts` ne valide que les références d'exercices des `LessonFooter` (ligne 47), jamais les `QuizBlock` ; consigné dans `docs/qa/quiz-todo.md` §3, cible `quizzes` maintenue à 11. Et les fiches introduisent **8 règles `orch.*` absentes du registre §7** (`harp-pedal`, `harp-hand`, `timp-tuning`, `mallet-choice`, `perc-player-count`, `organ-dynamics`, `organ-acoustics`, `trombone-slide`) — même statut que `orch.horn-agility`/`orch.horn-endurance`, déjà hors registre depuis la fiche cor ; sans implémentation à ce jour (`analyzers/orchestration.ts` et `data/instruments.ts` sont vides). `pnpm content:count` vert sur les 11 modules après coup. | (a) et (b) : demandes explicites de l'utilisateur, la seconde avec la source fournie — le pipeline normal du projet (rédaction en `docs/production/`, puis extraction vers `packages/content/`), donc une copie et non une production : la règle d'or 3.0.4 est respectée, contrairement au cas de la décision n°16 où il avait fallu arbitrer une rédaction dérivée faute de source. (c) est consigné parce que le périmètre livré diffère de celui qu'un lecteur de la demande initiale (et des scaffolds intermédiaires) aurait déduit — même famille que les décisions n°19 (M2 : 28 et non 29) et n°21 (`kind` manquant) : l'écart est tranché par la source, mais il doit rester traçable pour qu'un audit ultérieur ne rouvre pas la question du « module à 37 leçons ». (d) suit la doctrine des décisions n°8 et n°24 : un manque se consigne là où il se voit (`quiz-todo.md`, table d'extraction), il ne se comble pas en inventant du contenu que la source ne porte pas. |
| 28 | 2026-08-02 | **Les 15 quiz M5 manquants extraits de `docs/production/s29.5-m05Quizzes.md`** (75 items) — le manque ouvert le jour même par la décision n°27(d) est soldé sans délai, la source ayant été fournie ensuite. Extraction par script jetable (scratchpad, non versionné) avec **validation stricte avant écriture** : parsing JSON de chaque bloc, `id` conforme au motif et égal au nom de fichier, `interaction: "mc"`, 5 items, `why` présent sur chacun (verrou `count.ts` ligne 56), et `answer` toujours incluse dans ses propres `options` — cette dernière vérification n'est imposée par aucun verrou existant mais attrape la faute de frappe qui rendrait un item insoluble. **Collision d'IDs évitée, et c'est le point notable** : §29.5 signale en tête que les quiz du lot §29.4 portent l'ancienne numérotation et que quatre d'entre eux entrent en collision avec des fiches différentes — sans remapping, les quiz harpe/piccolo/flûte alto de ce lot auraient écrasé ceux de la flûte/clarinette/trompette. Le remapping exigé (`l06`→`l07`, `l07`→`l10`, `l08`→`l12`, `l09`→`l16`, `l10`→`l21`) avait déjà été appliqué la veille par la décision n°27(a) ; vérifié explicitement avant écriture que les 15 cibles étaient libres — aucun fichier existant n'a été écrasé. M5 : **26/26 quiz**, cible `count.ts` 11 → 26, les 26 `QuizBlock` du module résolvent tous vers un fichier réel. **Reste ouvert, non comblé** : les 15 items médias du lot (4 `capture`, 10 `playAsset`, 1 `play`) référencent **14 assets `asset:m05-lXX-…` qui n'existent pas** (`packages/content/assets/` ne contient que `la-remise`) — consigné dans `quiz-todo.md` §3, même famille que les 41 items de sa §1. Aucun format nouveau n'est introduit : `play`/`playAsset`/`capture` préexistent respectivement dans 98, 41 et 6 fichiers hors M5 (M6, M10, M11). `pnpm content:count` vert sur les 11 modules. | Source fournie par l'utilisateur, donc copie et non production — règle d'or 3.0.4 respectée, comme pour la décision n°27(b). La vérification anti-collision est consignée parce qu'elle était le vrai risque de l'opération : §29.5 l'annonçait comme un « point de production à traiter avant intégration », et une extraction naïve (écrire les 15 fichiers sans contrôle préalable) aurait détruit silencieusement trois quiz déjà commis — le type de perte qu'aucun verrou actuel ne rattrape, `count.ts` comptant des fichiers, pas leur contenu. La doctrine des items `▶` posée par §29.5 (`capture` pour les impossibilités physiques *qui ne s'entendent pas*, `playAsset` pour les contrastes de timbre, `play` pour ce qui s'entend partout) est reprise telle quelle dans `quiz-todo.md` : elle tranche pour M5 la question laissée ouverte par la décision n°5 pour les 41 items de M6/M7/M8/M10/M11. || 29 | 2026-08-03 | **Pipeline S5–S6 écrit (`evaluate`/`scoring`/`craft`/`feedback`), verrou n°2 ouvert sur M1+M2+M3 : 9/86 → 39/86.** Les cinq fichiers de `src/pipeline/` étaient vides. Décisions de spec introduites, non tranchées par le tutoriel : **(1)** *pas de Zod dans le moteur* — le tutoriel écrit « validation Zod » à l'étape 1 d'`evaluate`, mais `music-core` n'a aucune dépendance d'exécution (D-T2, déjà acté en tête de `constraints/schema.ts`) ; `validateSubmission` est écrite à la main, ce qui compte étant l'échec BRUYANT, pas l'outil. **(2)** *`compileSolution` ne rend pas `mono` sur une notation d'accords*, contrairement à la lettre du tutoriel (`notation → {kind:'mono'}`) : les familles `harmony.*` et `vl.*` ne s'appliquent qu'aux soumissions à voix (`appliesTo`), donc les 50 chorals du corpus auraient été jugés sans une seule règle d'harmonie — verrou vert et creux. Les empilements verticaux sont défaits en VOIX par registre ; limite assumée : attribution registrale et non contrapuntique. **(3)** *`computeCraft` peut rendre `null`* (DAW_MISSION, ANALYSIS, quiz) et `evaluate` REDISTRIBUE alors le poids `craft` de la rubric au prorata sur correctness et constraints — noter 0 punirait l'élève d'une limite du moteur, noter 1 lui offrirait des points gratuits. **(4)** *`Issue.ownerRuleId`* ajouté à `types.ts` : les analyseurs produisent des diagnostics plus fins que les règles enregistrées (`vl.direct-perfect` sous `vl.parallel-perfects`) ; le détail est conservé dans le rapport, le poids et la `pedagogy` viennent de la règle porteuse. **(5)** *`src/meter.ts`* créé pour casser le cycle checkers↔pipeline, et `barCount` compte les mesures AU PLUS PROCHE (cf. `docs/qa/lock2-run.md` A-2). **(6)** *`improvedVersion: null` au MVP*, comme prescrit ; le plafond ≤ 30 % est codé (`IMPROVED_VERSION_MAX_CHANGE`) et vérifié par `changedRatio`, testé dans `test/locks/report.test.ts`. **(7)** *Le registre de contraintes (annexe C) étendu à M3* : 42 clés ajoutées, toutes déclaratives — le verrou n°1 n'avait ouvert que M1+M2+M5+M9, et `checkConstraints` levait sur les specs M3. **Périmètre réel : 86 solutions, pas 87** — le Guide annonce M2 : 29, le corpus en porte 28 (écart de contenu, cohérent avec la décision n°19). **Le verrou n°2 n'est PAS vert et n'a pas été neutralisé** : 47 solutions restent rouges, chacune tracée dans `docs/qa/lock2-run.md` avec son poste de coût. Aucune constante de `scoring.ts` n'a été touchée. Le tag `v0.2-engine-core` n'est donc pas posé. | Demande explicite de l'utilisateur (« SEMAINES 5–6 … Fais ça »). Les dix correctifs de la remontée sont journalisés séparément dans `docs/qa/lock2-run.md`, comme l'exige le tutoriel (« ce journal EST ta preuve de calibrage ») ; ne figurent ici que les choix de conception qui engagent la suite. La décision (2) est la plus lourde et suit la doctrine de la mémoire projet « lire le tutoriel musicalement, pas littéralement » : appliquer sa lettre aurait produit un verrou qui passe en ne demandant rien. La décision (7) illustre l'inverse d'un raccourci : les 42 clés sont ajoutées en mode `declared`, qui dit à chaque exécution qu'elles ne sont pas mesurées — une clé silencieusement approuvée serait pire qu'une clé absente. Le refus de poser le tag et de neutraliser le verrou suit la consigne explicite du tutoriel : on ne baisse jamais un seuil pour faire passer une pièce. |
| 29 | 2026-08-03 | **Pipeline S5–S6 écrit (`evaluate`/`scoring`/`craft`/`feedback`), verrou n°2 ouvert sur M1+M2+M3 : 9/86 → 39/86.** Les cinq fichiers de `src/pipeline/` étaient vides. Décisions de spec introduites, non tranchées par le tutoriel : **(1)** *pas de Zod dans le moteur* — le tutoriel écrit « validation Zod » à l'étape 1 d'`evaluate`, mais `music-core` n'a aucune dépendance d'exécution (D-T2, déjà acté en tête de `constraints/schema.ts`) ; `validateSubmission` est écrite à la main, ce qui compte étant l'échec BRUYANT, pas l'outil. **(2)** *`compileSolution` ne rend pas `mono` sur une notation d'accords*, contrairement à la lettre du tutoriel (`notation → {kind:'mono'}`) : les familles `harmony.*` et `vl.*` ne s'appliquent qu'aux soumissions à voix (`appliesTo`), donc les 50 chorals du corpus auraient été jugés sans une seule règle d'harmonie — verrou vert et creux. Les empilements verticaux sont défaits en VOIX par registre ; limite assumée : attribution registrale et non contrapuntique. **(3)** *`computeCraft` peut rendre `null`* (DAW_MISSION, ANALYSIS, quiz) et `evaluate` REDISTRIBUE alors le poids `craft` de la rubric au prorata sur correctness et constraints — noter 0 punirait l'élève d'une limite du moteur, noter 1 lui offrirait des points gratuits. **(4)** *`Issue.ownerRuleId`* ajouté à `types.ts` : les analyseurs produisent des diagnostics plus fins que les règles enregistrées (`vl.direct-perfect` sous `vl.parallel-perfects`) ; le détail est conservé dans le rapport, le poids et la `pedagogy` viennent de la règle porteuse. **(5)** *`src/meter.ts`* créé pour casser le cycle checkers↔pipeline, et `barCount` compte les mesures AU PLUS PROCHE (cf. `docs/qa/lock2-run.md` A-2). **(6)** *`improvedVersion: null` au MVP*, comme prescrit ; le plafond ≤ 30 % est codé (`IMPROVED_VERSION_MAX_CHANGE`) et vérifié par `changedRatio`, testé dans `test/locks/report.test.ts`. **(7)** *Le registre de contraintes (annexe C) étendu à M3* : 42 clés ajoutées, toutes déclaratives — le verrou n°1 n'avait ouvert que M1+M2+M5+M9, et `checkConstraints` levait sur les specs M3. **Périmètre réel : 86 solutions, pas 87** — le Guide annonce M2 : 29, le corpus en porte 28 (écart de contenu, cohérent avec la décision n°19). **Le verrou n°2 n'est PAS vert et n'a pas été neutralisé** : 47 solutions restent rouges, chacune tracée dans `docs/qa/lock2-run.md` avec son poste de coût. Aucune constante de `scoring.ts` n'a été touchée. Le tag `v0.2-engine-core` n'est donc pas posé. | Demande explicite de l'utilisateur (« SEMAINES 5–6 … Fais ça »). Les dix correctifs de la remontée sont journalisés séparément dans `docs/qa/lock2-run.md`, comme l'exige le tutoriel (« ce journal EST ta preuve de calibrage ») ; ne figurent ici que les choix de conception qui engagent la suite. La décision (2) est la plus lourde et suit la doctrine de la mémoire projet « lire le tutoriel musicalement, pas littéralement » : appliquer sa lettre aurait produit un verrou qui passe en ne demandant rien. La décision (7) illustre l'inverse d'un raccourci : les 42 clés sont ajoutées en mode `declared`, qui dit à chaque exécution qu'elles ne sont pas mesurées — une clé silencieusement approuvée serait pire qu'une clé absente. Le refus de poser le tag et de neutraliser le verrou suit la consigne explicite du tutoriel : on ne baisse jamais un seuil pour faire passer une pièce. |
| 30 | 2026-08-03 | **Verrou n°1 (`completeness.test.ts`) est rouge depuis le commit de contenu M9 `67271d0`, et ce n'est pas une régression du moteur.** Son périmètre inclut `module-09`, qui vient de passer de 8 à 34 exercices ; les 26 exercices e09–e34 emploient ~157 clés de contraintes que `CONSTRAINT_SCHEMA` ne connaît pas (`accelerandoFinal`, `stingerPolicy`, `hitMap`, `emblemKit`…). C'est exactement ce que le verrou est censé faire — signaler qu'une contrainte écrite n'est vérifiée par personne — et le doc source `s23.3-exModule9Suite.md` l'annonce en §Décisions de spec (« clés de contraintes nouvelles »). **Non corrigé ici** : c'est le travail d'ouverture du verrou n°1 sur le lot M9 V1, pas du verrou n°2. | Constaté en exécutant la suite complète après le pipeline S5–S6. Consigné plutôt que corrigé au passage : ajouter 157 clés déclaratives sans lire leur doc de spec reviendrait à faire taire le verrou, ce que la décision n°29(7) refuse explicitement pour les 42 clés M3 qui, elles, ont été lues. |
| 31 | 2026-08-04 | **Le cadrage à quatre voix de la n°29(2) est CONFIRMÉ ; son déclencheur est resserré. Le corpus se scinde en deux lots.** La n°29(2) reste juste sur le fond, et la source le dit : l'exemple canonique du Guide (§2, `m03-e02-neapolitan-phrase`) déclare `minVoices: 4, maxVoices: 4` et ses `authorNotes` parlent en vocabulaire SATB (« doublure de basse du ♭II⁶ », « octave directe finale couverte par l'exception soprano-par-degré »). C'est l'auteur du Manuel qui pense ces chorals à quatre voix indépendantes. Ce qui était faux, c'est l'APPLICATION EN BLOC : le déclencheur d'origine était « la notation empile des hauteurs », sans regarder ce que la spec déclare comme largeur de voix. `m03-e15-the-veil-the-blade-the-mass` (`minVoices: 1, maxVoices: 8`, `styleProfile: impressionist`, leçon sur le cluster) passait donc par `unstackVoices` et était jugé par des règles écrites pour quatre voix indépendantes. **Nouveau déclencheur** (`voiceTextureOf`, deux témoins concordants) : ce que la spec DÉCLARE — `minVoices`/`maxVoices` compatibles avec l'effectif observé et FERMÉS, une plage ouverte type 1–8 ou 2–7 annonçant un travail de densité — et ce que la pièce TIENT — effectif ≥ 2, aucune verticalité plus épaisse que la norme, aucune plus mince sauf la dernière (finir à moins de voix qu'on n'en tenait est un geste de composition, revendiqué en `authorNotes` par m03-s06 et m03-s10, et rien ne vient après elle que le décalage puisse corrompre). **LOT A = 73 solutions**, jugées par `harmony.*`/`vl.*`. **LOT B = 13 solutions** hors périmètre. **Le lot B échoue BRUYAMMENT** (`UnrepresentableTexture`, nommé à chaque exécution du verrou dans le tableau de bord) et n'est PAS replié en `mono` : mesuré, `mono` faisait passer 16 des 17 candidates sans qu'une seule règle d'harmonie ne leur soit posée — vert et creux, ce que la n°29(2) refusait déjà. Aucun `Submission['kind']` ne décrit une texture à densité variable (`voices` suppose un effectif, `mono` une ligne, `layers` un empilement de sound design) : le verrou ne peut donc pas dépasser **73/86** avant qu'une forme de soumission adaptée existe, et le tag `v0.2-engine-core` n'est toujours pas posé. **Conséquence chiffrée** : 42 pleinement verts avant, 42 après — mais quatre pièces qui passaient creux sont sorties (m03-e10, m03-e12 [creature], m03-e13, m03-e15) et quatre pièces réellement jugées sont entrées (m01-e35, m03-e05 [ger6-v7], m03-e09, m03-e18 [fonctionnel-etendu]). | Le cadrage n'a pas été rouvert : il était déjà tranché par la source, et le rouvrir aurait été revenir sur un point réglé. Le défaut n'était pas la lecture à quatre voix mais l'absence de test d'applicabilité — la même erreur de forme que la n°29(2) corrigeait dans l'autre sens. Les six correctifs de règles qui suivent (F-1 à F-5, journal `docs/qa/lock2-run.md`) n'ont été instruits qu'APRÈS ce resserrement, sur le terrain propre : calibrer `vl.*` sur un lot contenant des clusters à huit notes aurait produit des exceptions taillées pour du bruit. Aucun seuil de `scoring.ts` ni aucun poids de profil n'a été touché. |
